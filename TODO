-don't get resolution, etc. on models which don't support it
-error out on unsupported models
-cursor support (buttons + scrollwheel)
-tilt support
-factor out: init_completion .. send .. wait for completion into a helper
-cleanups (check-patch, read the code and fix whatever stands out)
-fixup ABS_MISC reporting, set evbit for it use set_abs
-add properties:
 -cintiq always covers a screen ? Only set INPUT_PROP_DIRECT
 -everything else is a regular tablet, set INPUT_PROP_DIRECT + INPUT_PROP_POINTER
-timeouts maybe too short if HZ == 1000
-fixup copyright header, got permisson from Julian for this +
 Signed-off-by: Julian Squires <julian@cipht.net>
